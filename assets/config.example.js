/*
  Copie este arquivo e renomeie para:

  config.js

  Depois preencha com os dados do seu próprio projeto Supabase.

  Onde encontrar esses dados:
  Supabase → Project Settings → API

  Use apenas:
  - Project URL
  - anon public key ou publishable key

  Nunca coloque a chave service_role no navegador.
*/

window.APP_CONFIG = {
  SUPABASE_URL: "https://SEU-PROJETO.supabase.co",
  SUPABASE_ANON_KEY: "SUA_CHAVE_PUBLICA_DO_SUPABASE",

  // Valor usado apenas como referência para preencher o campo de juros.
  DEFAULT_SELIC_PERCENT: 10.5
};
